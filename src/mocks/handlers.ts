import { abandonment, abandonments } from './abandonments';
import { banners } from './banners';
import { shelter, shelterCount, shelters } from './shelters';

export const handlers = [banners, abandonments, abandonment, shelters, shelter, shelterCount];
