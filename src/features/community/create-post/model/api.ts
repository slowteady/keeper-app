import { TCreatePostDto } from '@/entities';

export const createPost = async (data: TCreatePostDto) => {
  // try {
  //   const response = await api.post('/posts', data);
  //   return response.data;
  // } catch (error) {
  //   console.error('Failed to create post:', error);
  //   throw error;
  // }
};
