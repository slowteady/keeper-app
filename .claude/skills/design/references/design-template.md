# Design 산출물 템플릿

`docs/design/<feature>.md` 작성 시 아래 골조를 그대로 복사해서 채운다.

비어두지 말 것. 정보가 없으면 `TBD — <어떤 의사결정 대기 중인지>` 로 명시.

---

```markdown
# Design: <기능명>

## 1. 메타

- 작성일: YYYY-MM-DD
- 상태: 초안 / 리뷰 / 확정 / 보류
- 입력 PRD: docs/prd/<feature>.md
- Figma URL: <있다면 / 없으면 "없음 — 컴포넌트 조립 기반">

## 2. 화면 목록 + 흐름

화면마다 한 줄 정의 + 진입/이탈 경로.

| 화면 ID | 화면명 | 진입 경로 | 다음 화면 |
| ------- | ------ | --------- | --------- |
| S1      | <name> | <from>    | <to>      |

흐름이 복잡하면 텍스트 다이어그램 또는 외부 Figma flow 링크.

## 3. 컴포넌트 매핑

### S1: <화면명>

컴포넌트 트리:
```

<루트 컨테이너>
├── <컴포넌트 A>
└── <컴포넌트 B>
└── <컴포넌트 C>

```

매핑 표:

| 컴포넌트 | 출처 | 용도 | 재활용 여부 |
|---|---|---|---|
| <name> | shared/ui / features/<x> / entities/<x> / 라이브러리(<name>) / 신규 | <한 줄> | 재활용 / 신규 |

### S2: <화면명>
...

### 신규 컴포넌트 사유

- <컴포넌트명>: <왜 기존 재활용이 안 되는지>
- ...

## 4. FSD 프론트 슬라이스 매핑

신규 컴포넌트의 배치:

| 컴포넌트 | 슬라이스 | 파일 경로 |
|---|---|---|
| <name> | shared/ui / entities/<x>/ui / features/<x>/ui / widgets/<x>/ui / app/<route> | src/... |

## 5. 의존성

- 라이브러리 추가 설치 필요: <있으면 / 없으면 "없음">
- 다른 슬라이스 변경 영향: <있으면 / 없으면 "없음">
- 선행 작업: <기능 의존이 있으면>

## 6. ADR + Open Issues

### 결정 기록

| 결정 | 옵션 | 채택 | 사유 |
|---|---|---|---|
| <결정 이름> | 재활용 A vs 신규 B | <채택> | <왜> |

### Open Issues

- TBD — <무엇을 결정해야 하는지>

## 참고

- PRD: `docs/prd/<feature>.md`
- 백로그: `docs/backlog/<feature>.md`
- 외부 UI BP: <링크>
- Figma: <링크 또는 "없음">
```
