import { StyleSheet, View } from 'react-native';

interface MainReviewSectionProps {
  //
}

const MainReviewSection = ({}: MainReviewSectionProps) => {
  return <View></View>;
};

export default MainReviewSection;

const styles = StyleSheet.create({});
