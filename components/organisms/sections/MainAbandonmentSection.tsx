import { StyleSheet, View } from 'react-native';

interface MainAbandonmentSectionProps {
  //
}

const MainAbandonmentSection = ({}: MainAbandonmentSectionProps) => {
  return <View></View>;
};

export default MainAbandonmentSection;

const styles = StyleSheet.create({});
