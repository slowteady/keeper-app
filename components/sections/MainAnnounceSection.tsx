import { StyleSheet, View } from 'react-native';

interface MainAnnounceSectionProps {
  //
}

const MainAnnounceSection = ({}: MainAnnounceSectionProps) => {
  return <View></View>;
};

export default MainAnnounceSection;

const styles = StyleSheet.create({});
